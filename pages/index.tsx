import styled from 'styled-components';

const Home: React.FunctionComponent = () => (
  <div className="test">
    <Title>Test</Title>
  </div>
);

const Title = styled.h1`
  color: red;
`;

export default Home;
